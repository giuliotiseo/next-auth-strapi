import { Metadata } from "next"

// Meta ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
export const metadata:Metadata = {
  title: "Next Auth Strapi",
  description: "Next auth strapi website"
}

export default function Home() {
  return (
    <div>
      <p>Home</p>
    </div>
  )
}
